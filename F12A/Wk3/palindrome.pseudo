function Palindrome:
    Input: String S of length n
    Output: Whether S is a Palindrome

    for i from 0 to n / 2:
        if S[i] == S[n - 1 - i]:
            i = i + 1
        else:
            return false

    return true

// O(n)