function is_palindrom(String S, length n) {
    let i = 0;

    while i < n/2:
        if S[i] = S[n - 1 - i]:
            i = i + 1
        else
            return False
    
    return True
}

O(n) * O(1) = O(n)